services:
  mysql:
    image: mysql
    command: --default-authentication-plugin=mysql_native_password
    ports:
      - '3306:3306'
    environment:
      MYSQL_ROOT_PASSWORD: 'unitofworkexample'
      MYSQL_DATABASE: 'unitofworkexample'
